npm init
npm install express mongoose cors dotenv bcrypt bull cookie-paraser ejs express-validator jsonwebtoken morgan multer node-cron nodemailer

mkdir controllers && mkdir routes && mkdir middlewares && mkdir models 